#include <iostream>
#include "Prostokat.h"
#include "Kolo.h"
#include "Tablica.h"
#include "Edytor.h"

using namespace std;

int main()
{
   /* Prostokat *kolo = new Prostokat(100, 50);
    Kolo *prostokat = new Kolo(46);

    vector<int> przes;
    przes.push_back(1);
    przes.push_back(2);
    przes.push_back(3);
    kolo->modyfikuj(przes);


    Tablica tablica;
    tablica.dodaj(kolo);
    tablica.dodaj(prostokat);

    tablica.rysuj(); */
   Edytor edytor;
    edytor.konsola();

    return 0;

}
